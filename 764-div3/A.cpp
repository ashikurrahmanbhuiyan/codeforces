#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	freopen("A.txt","r",stdin);
	int t;cin>>t;
	while(t--){
	   int a,b,c;cin>>a>>b>>c;
	   if (a == 1 && b == 1)
	   {
	   	cout<<"NO"<<endl;
	   }
	   else if (b == 1 && c == 1)
	   {
	   	cout<<"NO"<<endl;
	   }
	      else
	   	cout<<"YES"<<endl;

	}
	
}